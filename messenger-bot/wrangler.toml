name = "counseling-messenger-bot"
main = "src/index.js"
compatibility_date = "2026-05-14"

[vars]
# IMPORTANT: Sensitive keys (GEMINI_API_KEY, PAGE_ACCESS_TOKEN, etc.)
# MUST be set as Secrets in the Cloudflare Dashboard or via 'wrangler secret put'
# to prevent them from being leaked and disabled by GitHub.
VERIFY_TOKEN = "CareMeJesus2026"

[[kv_namespaces]]
binding = "CHAT_LOGS"
id = "dd287c3be3ba437ab4ab22b2f53145d3"

[triggers]
crons = ["*/5 * * * *"]
